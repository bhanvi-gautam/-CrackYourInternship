bool isPossible(long long a[], long long b[], int n, long long k) {
        // Your code goes here
        for(long long int i=0;i<n;i++){
            if(a[i]+b[i]>=k){
                return true;
            }
        }
        return false;
    }
